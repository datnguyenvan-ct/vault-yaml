vault_address: https://vault-cloud.chotot.org
vault_token_env: CHOTOT_VAULT_GCP_DEV_TOKEN

k8s_cluster_env: CHOTOT_GKE_DEV
vault_secret_engine: gke-dev
vault_k8s_auth_path: gke-dev

services:
  - name: ct-sre-demo
    namespace: default
    action: create
    secrets:
      - TOKEN
      - GITHUB
